sources: ../test_data
processed: data/processed
training_data: data/training
model:
  model_id: Qwen/Qwen2.5-0.5B-Instruct
  revision: 7ae557604adf67be50417f59c2c2f167def9a775
  device: auto
  max_new_tokens: 512
  max_input_tokens: 4096
  seed: 483
  quantize_4bit: false
  adapter_path: null
training:
  output_dir: artifacts/adapters/freight-qlora
  checkpoint_dir: artifacts/checkpoints/freight-qlora
  seed: 483
  epochs: 1
  learning_rate: 0.0002
  batch_size: 1
  gradient_accumulation_steps: 8
  max_length: 2048
  lora_r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  target_modules: all-linear
matching:
  # All matching settings are provisional screening assumptions.
  cargo_fraction: null
  required_quantity: max
  require_same_zone: false
  blocked_statuses: [FIXED, ON SUBS]
  allowed_statuses: [OPEN]
  max_report_age_days: null
  weights:
    capacity: 0.5
    window: 0.3
    geography: 0.2
  # Optional curated coordinates only; straight-line distance is not a sea route.
  coordinates: {}
