# External raw sources

Authoritative workbooks remain in `../../../test_data` relative to this directory:
`SMU_Order_data_TEST.xlsx` and `SMU_Tonnage_data_TEST.xlsx`.
The actual path is configured in `../../configs/default.yaml` as `../test_data`,
relative to the freight-ai-poc project root. Do not add generated records here.
